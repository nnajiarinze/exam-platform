name: Deploy hosted authentication experience

on:
  workflow_dispatch:
    inputs:
      commit_sha:
        description: Full tested commit SHA with published auth images
        required: true
      public_domain:
        description: Authoritative hosted HTTPS hostname
        required: true
        default: api.tinkona.com

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: hosted
    steps:
      - name: Validate immutable inputs
        env:
          COMMIT_SHA: ${{ inputs.commit_sha }}
          PUBLIC_DOMAIN: ${{ inputs.public_domain }}
          IMAGE_REGISTRY: ghcr.io/${{ github.repository_owner }}
        run: |
          [[ "${COMMIT_SHA}" =~ ^[0-9a-f]{40}$ ]]
          [[ "${PUBLIC_DOMAIN}" == api.tinkona.com ]]
          [[ "${IMAGE_REGISTRY}" =~ ^ghcr\.io/[A-Za-z0-9][A-Za-z0-9-]*$ ]]
          [[ "${IMAGE_REGISTRY}" != ghcr.io/owner ]]
      - name: Configure protected Hetzner SSH
        env:
          SSH_PRIVATE_KEY: ${{ secrets.HOSTED_SSH_PRIVATE_KEY }}
          KNOWN_HOSTS: ${{ secrets.HOSTED_KNOWN_HOSTS }}
        run: |
          install -d -m 700 ~/.ssh
          printf '%s\n' "${SSH_PRIVATE_KEY}" >~/.ssh/id_ed25519
          printf '%s\n' "${KNOWN_HOSTS}" >~/.ssh/known_hosts
          chmod 600 ~/.ssh/id_ed25519 ~/.ssh/known_hosts
      - name: Deploy only gateway, identity provider, and identity readiness backend
        env:
          COMMIT_SHA: ${{ inputs.commit_sha }}
          PUBLIC_DOMAIN: ${{ inputs.public_domain }}
          HOSTED_HOST: ${{ secrets.HOSTED_HOST }}
          HOSTED_USER: ${{ secrets.HOSTED_USER }}
          KEYCLOAK_GOOGLE_CLIENT_ID: ${{ secrets.KEYCLOAK_GOOGLE_CLIENT_ID }}
          KEYCLOAK_GOOGLE_CLIENT_SECRET: ${{ secrets.KEYCLOAK_GOOGLE_CLIENT_SECRET }}
          KEYCLOAK_GOOGLE_ENABLED: ${{ vars.KEYCLOAK_GOOGLE_ENABLED != '' && vars.KEYCLOAK_GOOGLE_ENABLED || secrets.KEYCLOAK_GOOGLE_ENABLED }}
          KEYCLOAK_APPLE_ENABLED: ${{ vars.KEYCLOAK_APPLE_ENABLED != '' && vars.KEYCLOAK_APPLE_ENABLED || secrets.KEYCLOAK_APPLE_ENABLED }}
          KEYCLOAK_APPLE_SERVICES_ID: ${{ vars.KEYCLOAK_APPLE_SERVICES_ID != '' && vars.KEYCLOAK_APPLE_SERVICES_ID || secrets.KEYCLOAK_APPLE_SERVICES_ID }}
          KEYCLOAK_APPLE_TEAM_ID: ${{ vars.KEYCLOAK_APPLE_TEAM_ID != '' && vars.KEYCLOAK_APPLE_TEAM_ID || secrets.KEYCLOAK_APPLE_TEAM_ID }}
          KEYCLOAK_APPLE_KEY_ID: ${{ vars.KEYCLOAK_APPLE_KEY_ID != '' && vars.KEYCLOAK_APPLE_KEY_ID || secrets.KEYCLOAK_APPLE_KEY_ID }}
          KEYCLOAK_APPLE_PRIVATE_KEY_BASE64: ${{ secrets.KEYCLOAK_APPLE_PRIVATE_KEY_BASE64 }}
          IMAGE_REGISTRY: ghcr.io/${{ github.repository_owner }}
        run: |
          payload_json="$(jq -cn \
            --arg cid "${KEYCLOAK_GOOGLE_CLIENT_ID}" \
            --arg csecret "${KEYCLOAK_GOOGLE_CLIENT_SECRET}" \
            --arg enabled "${KEYCLOAK_GOOGLE_ENABLED}" \
            --arg apple_enabled "${KEYCLOAK_APPLE_ENABLED}" \
            --arg apple_services_id "${KEYCLOAK_APPLE_SERVICES_ID}" \
            --arg apple_team_id "${KEYCLOAK_APPLE_TEAM_ID}" \
            --arg apple_key_id "${KEYCLOAK_APPLE_KEY_ID}" \
            --arg apple_private_key_base64 "${KEYCLOAK_APPLE_PRIVATE_KEY_BASE64}" \
            '{
              keycloak_google_client_id:$cid,
              keycloak_google_client_secret:$csecret,
              keycloak_google_enabled:$enabled,
              keycloak_apple_enabled:$apple_enabled,
              keycloak_apple_services_id:$apple_services_id,
              keycloak_apple_team_id:$apple_team_id,
              keycloak_apple_key_id:$apple_key_id,
              keycloak_apple_private_key_base64:$apple_private_key_base64
            }')"
          printf '%s' "${payload_json}" | \
            ssh -o BatchMode=yes "${HOSTED_USER}@${HOSTED_HOST}" \
            "set -Eeuo pipefail; cd /opt/citizenship-platform/repo; git fetch --quiet origin '${COMMIT_SHA}'; git checkout --detach '${COMMIT_SHA}'; ./infrastructure/hetzner/deploy-hosted-auth.sh '${COMMIT_SHA}' '${PUBLIC_DOMAIN}' '${IMAGE_REGISTRY}'"
      - name: Verify public authentication surface
        env:
          PUBLIC_DOMAIN: ${{ inputs.public_domain }}
        run: |
          set -Eeuo pipefail
          base="https://${PUBLIC_DOMAIN}"
          issuer="${base}/auth/realms/exam-platform"
          curl --fail --silent --show-error "${issuer}/.well-known/openid-configuration" >discovery.json
          jq -e --arg issuer "${issuer}" '.issuer==$issuer and (.authorization_endpoint|startswith($issuer+"/"))' discovery.json
          curl --fail --silent --show-error --get --output login.html \
            --data-urlencode client_id=mobile-app \
            --data-urlencode redirect_uri=sveastudy://auth/callback \
            --data-urlencode response_type=code \
            --data-urlencode scope='openid profile email' \
            --data-urlencode code_challenge=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa \
            --data-urlencode code_challenge_method=S256 \
            --data-urlencode ui_locales=sv \
            "$(jq -r .authorization_endpoint discovery.json)"
          grep -q '<title>Logga in till Svea Study</title>' login.html
          curl --fail --silent --show-error "${base}/healthz" >/dev/null
